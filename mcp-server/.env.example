# Gmail API Configuration (Server-side)
GMAIL_CLIENT_ID=your-gmail-client-id.apps.googleusercontent.com
GMAIL_CLIENT_SECRET=your-gmail-client-secret
GMAIL_REDIRECT_URI=http://localhost:3001/auth/callback

# MCP Server Configuration
MCP_SERVER_PORT=3001
MCP_SERVER_HOST=localhost

# Security
JWT_SECRET=your-super-secret-jwt-key-for-sessions
CORS_ORIGIN=http://localhost:3000

# Gmail API Scopes
GMAIL_SCOPES=https://www.googleapis.com/auth/gmail.readonly

# Logging
LOG_LEVEL=info

# Instructions:
# 1. Copy this file to .env in the mcp-server directory
# 2. Set up Gmail API credentials in Google Cloud Console
# 3. Update the values above with your actual credentials
# 4. Generate a strong JWT secret for session management
# 5. Ensure CORS_ORIGIN matches your frontend URL 