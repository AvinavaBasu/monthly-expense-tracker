import React from 'react'
import ReactDOM from 'react-dom/client'
import { ExpenseMapper } from './components/ExpenseMapper'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ExpenseMapper />
  </React.StrictMode>,
) 